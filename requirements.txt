# Microsoft Style Guide MCP Server and Client Requirements
# 
# Core Python version requirement: Python 3.8+
# This project requires Python 3.8 or higher for asyncio and typing support
# 
# SUMMARY OF DEPENDENCIES:
# - mcp: Model Context Protocol library (with fallback for dev mode)
# - aiohttp: Required for web version HTTP requests to Microsoft Style Guide
# - regex: Enhanced regex support (fallback to built-in 're' if unavailable)
# 
# All other dependencies are from Python standard library:
# - asyncio, json, logging, re, sys, argparse (core functionality)
# - typing, pathlib (type hints and file operations) 
# - urllib.parse (URL encoding for web requests)

# Core MCP dependencies
# Note: MCP library might not be available in all environments
# The code includes fallback implementations for development mode
mcp>=1.0.0

# Web capabilities for fetching live style guide content (required for mcp_server_web.py)
# Also provides async HTTP client functionality
aiohttp>=3.8.0

# Additional dependencies for text analysis and enhanced pattern matching
# The 'regex' module provides more advanced regex features than built-in 're'
regex>=2023.10.3

# HTTP/URL utilities (built into Python but included for clarity)
# urllib.parse is used for URL encoding and manipulation

# Development and testing dependencies (optional but recommended)
# Uncomment these for development environment setup

# Testing framework
# pytest>=7.4.3
# pytest-asyncio>=0.21.1

# Code formatting and linting
# black>=23.9.1
# flake8>=6.1.0
# mypy>=1.6.1

# Enhanced text analysis (optional - uncomment if advanced text analysis is needed)
# nltk>=3.8.1
# textstat>=0.7.3

# HTML parsing for better web scraping (optional - for enhanced web functionality)
# beautifulsoup4>=4.12.0

# Enhanced logging and CLI experience (optional)
# colorlog>=6.7.0
# rich>=13.6.0

# Security and networking (optional but recommended for production)
# certifi>=2023.7.22  # For SSL certificate validation
# charset-normalizer>=3.3.0  # For character encoding detection